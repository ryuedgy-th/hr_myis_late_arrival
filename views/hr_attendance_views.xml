<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extended Attendance Tree View -->
    <record id="view_attendance_tree_extended" model="ir.ui.view">
        <field name="name">hr.attendance.tree.extended</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.view_attendance_tree"/>
        <field name="arch" type="xml">
            <field name="check_out" position="after">
                <field name="is_late"/>
                <field name="late_minutes" attrs="{'invisible': [('is_late', '=', False)]}"/>
            </field>
        </field>
    </record>

    <!-- Extended Attendance Form View -->
    <record id="view_attendance_form_extended" model="ir.ui.view">
        <field name="name">hr.attendance.form.extended</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.hr_attendance_view_form"/>
        <field name="arch" type="xml">
            <field name="check_out" position="after">
                <field name="is_late"/>
                <field name="late_minutes" attrs="{'invisible': [('is_late', '=', False)]}"/>
                <field name="late_reason" attrs="{'invisible': [('is_late', '=', False)]}"/>
                <field name="manager_approved" attrs="{'invisible': [('is_late', '=', False)]}"/>
            </field>
        </field>
    </record>
</odoo>
